type Recipe {
  id: ID!
  title: String!
  description: String
}
extend type Query {
  getRecipes: [ Recipe! ]
  getRecipe(id: ID!): Recipe
}
input RecipeInput {
  title: String!
  description: String
}
extend type Mutation {
  addRecipe(input: RecipeInput!): Recipe
}